# ES to ByteHouse 同步配置

# ========== Elasticsearch 配置 ==========
ES_HOST=http://localhost:9200
ES_USER=admin
ES_PASSWORD=your_es_password

# ========== ByteHouse 配置 ==========
BYTEHOUSE_HOST=your_bytehouse_host
BYTEHOUSE_PORT=19000
BYTEHOUSE_USER=bytehouse
BYTEHOUSE_PASSWORD=your_bytehouse_password
BYTEHOUSE_SECURE=true

# ========== 同步配置 ==========
# 目标数据库名
TARGET_DATABASE=es_migration

# 要同步的索引模式，支持通配符，多个索引用逗号分隔
# 例如: user_info_prod 或 * 或 logs-*
INDEX_PATTERN=test_index

# 每批处理的数据量
BATCH_SIZE=1000
SCROLL_SIZE=1000
SCROLL_TIMEOUT=5m

# 增量同步间隔（秒）
INCREMENTAL_INTERVAL=60

SKIP_FULL_SYNC=false 

# 是否存储原始 _source JSON（默认 false，节省空间）
# 设为 true 可保留完整原始文档
STORE_SOURCE=false

# ========== 日志配置 ==========
# 日志级别: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
# 日志文件路径（可选，如果不设置则只输出到控制台）
LOG_FILE=

# ========== 告警通知配置 ==========
# 飞书 Webhook 地址（用于发送 WARNING 和 ERROR 级别的告警）
FEISHU_WEBHOOK=
